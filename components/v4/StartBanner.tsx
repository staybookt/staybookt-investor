/* GONE. This was the "Before we meet, we try to hire you" CTA card, and it closed
 * every page on the site. It is replaced by <HeroCta />, the image scene the homepage
 * already ended on: the life the product is for, and one button. One closing CTA,
 * everywhere (Jacob, live review, July 2026).
 *
 * The mystery-shop hook itself is not dead. It still runs as a single line under the
 * homepage hero button, which is where it earns its keep.
 *
 * Emptied rather than deleted because the tooling that runs this repo can write files
 * but cannot delete them. `git rm` it. Do not restore this card. */
export {};

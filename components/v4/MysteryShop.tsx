/* GONE. This was the mystery-shop "call sheet": we call your line, we text your
 * listing, we search for you, we try to book a job. It went unused when /start became
 * a booking page, and the whole idea came off the site shortly after (Jacob, July 2026).
 *
 * Emptied rather than deleted because the tooling that runs this repo can write files
 * but cannot delete them. `git rm` it. Do not restore it. */
export {};

/* GONE. This was the "Under the hood" section on /whats-included: the customer record,
 * the assistant, and the numbers, as three mockups with a customer-list illustration.
 *
 * Richard: "I don't think the customer record section works. I would be inclined to just
 * go with the top chart and keep it simple." (July 2026.) The assistant and the customer
 * record are rows in the comparison chart now, which is where a person actually reads
 * them, so the section was restating the table directly above it.
 *
 * Emptied rather than deleted because the tooling that runs this repo can write files but
 * cannot delete them. `git rm` it. */
export {};

/* NEUTRALISED. Dead code from the killed offer, kept compiling only because it
 * imported another dead file. Imported by no live route.
 *
 * Emptied rather than deleted because the tooling that runs this repo can write
 * files but cannot delete them. It should be `git rm`d with the rest of the dead
 * tree. The live offer lives in lib/site.ts and nowhere else.
 */
export {};

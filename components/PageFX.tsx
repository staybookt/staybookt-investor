/* ============================================================
 * PageFX.tsx — RETIRED in v14.2.
 *
 * Page transition + cursor follower effects from an earlier
 * iteration. Never imported on the four live sub-pages or the
 * homepage after the v8.x architecture pass. Kept as an empty
 * file to make stale imports fail loudly rather than silently.
 *
 * Pull from git history if needed.
 * ============================================================ */
export {};

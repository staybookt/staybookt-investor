/* ============================================================
 * OperatingSystem.tsx — RETIRED in v14.2.
 *
 * Contents superseded by:
 *   - FlywheelOS.tsx (homepage 7-stage pinned-scroll)
 *   - CustomerProfile.tsx (PilotOneCompact, MeasurementPlan)
 *
 * Original exports (StayBooktOS, NinetyDayPlaybook, WeeklyOps,
 * CustomerOutcomes) all contained fabricated "Tim, today" stats
 * from before the v11 honesty pass. Kept here as a deliberate
 * empty file so any stale import surfaces as a clear build error
 * rather than silently rendering the old lies.
 *
 * Pull from git history if you need to resurrect any of it.
 * ============================================================ */
export {};
